# Phase 8 report — Polish, formal hooks, packaging, RTM

Final phase. Added: the command palette (UI-4 P1, Ctrl+K — type-to-filter actions for
open/re-run/views/themes); the visualizer slack overlay (VZ-1 "on demand" — backward
pass in `core/viz.compute_slack`, critical path verified to show zero slack); NF-2
golden-model throughput test (measured well above the 100 k op-evals/s floor);
SymbiYosys formal hooks (FV-1: generated `formal_top.sv` asserts valid-propagation
delay equals the cost-model latency via a reference shift register, RP-1 ranges
rendered as SVA assumes, plus the `.sby` project; FV-2: yosys/sby absence raises an
actionable message); timeline bisection rendering exercised under pytest-qt (BI-3);
a UI-6 findings-table test (sort, tag filter, click-through); README/user docs.

Traceability (§9): `pytest --rtm-out=docs/rtm.csv` regenerates the RTM from
`@pytest.mark.req` markers with live outcomes; `tools/check_rtm.py` (and `make rtm`)
enforces that **every P0 requirement has a passing test** — 42/44 pass locally and
the remaining two (CS-4, CG-3) are tool-gated tests that skip visibly without
Verilator and execute in the CI `verify-with-tools` job.

Gate evidence: full regression green (227 passed, 2 tool-skipped; all prior phases'
tests re-run); fresh-clone install-and-run verified (git clone → venv →
`pip install -e .` → `pipeforge-cli audit` and an offscreen GUI window both work);
final Gruvbox Dark Soft screenshot set regenerated in `docs/screens/`.

- Tests: 227 passed, 2 skipped. Coverage ≈88 % overall, core ≈92 % (gates: 80/90 %).
  ruff + ruff format + mypy --strict(core): clean. `make verify` is the single gate.
- Known deviations from the SRS at project exit:
  - Seed assets were authored, not provided (documented in Phase 0/1 reports).
  - CG-5 (P1 rewrite toggles), RP-3 GUI surfacing, VCD-based probe extraction for
    bisection, and dedicated GUI views for linter/cosim/bisect/golden (CLI covers
    them headlessly per §2.1) remain as future work; placeholders state how to begin.
