# Phase 0 report — Scaffold

Built the §3.2 repository layout: src-layout `pipeforge` package with all eleven `core/`
subpackages, `services/`, `gui/` (theme/views/widgets), `pyproject.toml` with entry points
(`pipeforge`, `pipeforge-cli`), ruff/mypy/pytest configuration, GitHub Actions CI (plain +
external-tools jobs per §8.2), and a `make verify` gate target. The theme token engine
(TH-1) loads and validates JSON semantic-token themes; four themes are bundled
(Gruvbox Dark Soft per Appendix A — normative values pinned by test — plus Dark Hard,
Light, High Contrast). Architecture tests (§8.5 a–d) are green; `pipeforge-cli --version`
works; the empty-window pytest-qt smoke test passes offscreen.

- Tests: 10 passed. Coverage: 90.6 % (gate: 60 %). ruff + ruff format + mypy --strict(core): clean.
- Known deviations: the SRS-referenced seed assets (`seed/matlib_audit.py`, `seed/example.m`)
  were **not present in the delivered repository**; they will be authored at the start of
  Phase 1 from the behavior specified in §1.5/R2 and then treated as the frozen seed.
